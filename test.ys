# read_aiger tests/aig/i2c.aig
# read_aiger tests/pango/design_13.aig
# read_blif /Users/yoyowang/Documents/Work/FudanCAD/paper2026/logic_benchmarks/blif/mcnc-alu2.blif
# read_aiger sm4_comb.aag
read_aiger alu2.aig
# read_verilog original.v
proc; opt; techmap; opt;
# 将BLIF逻辑转换为仅含AND/NOT的AIG形式，避免lut6d_map遇到$_MUX_等不支持单元
aigmap
hierarchy -check -auto-top
# write_verilog original.v
# logger -debug
stat
lut6d_map
# nlutmap
clean
stat
# write_verilog mapped_tc1.v
